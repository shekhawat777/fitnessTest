import React from "react";

const ViewAppointmentView = ({ data, fields }) => {
  return (
    <div style={{ margin: "auto" }}>
      Write your View Appointment page code
    </div>
  );
};

export default ViewAppointmentView;
