import React from 'react';
const TheFooter = () => {
    return (
        <div style={{ margin: "auto" }}>
            Write your Footer
        </div>

    );
};

export default React.memo(TheFooter);
